#pragma once
//#include "../Hosting.h"
//extern Hosting g_hosting;
//_gamepadClient.sendMessage(inputGuest, inputGuestMsg);

//tryAssignGamepad(guest, padId, slots, message.type == MESSAGE_KEYBOARD, guestPrefs);


/*gamepadclient.reduceUntilFirst([&](AGamepad* pad) {
	if (guest.userID == pad->owner.guest.userID)
	{
		slots++;
	}
}*/

/*gamepadclient.GuestPreferences guestPrefs = GuestPreferences();
findPreferences(guest.userID, [&guestPrefs](GuestPreferences& prefs) {
	guestPrefs = prefs;
	});*/

/*ParsecMessage message = {};
	message.type = ParsecMessageType::MESSAGE_GAMEPAD_BUTTON;
	message.gamepadButton = button;
*/